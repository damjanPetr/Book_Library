{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "tailwind",
      "type": "shell",
      "command": "npx tailwindcss -i ./main.css -o ./style.css --watch",
      "runOptions": { "runOn": "folderOpen" }
    }
  ]
}
